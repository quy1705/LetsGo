English: The internal directory will contain the ancillary non-application-specific code used in the
project. We’ll use it to hold potentially reusable code like validation helpers and the SQL
database models for the project.

VietNam:
Thư mục nội bộ sẽ chứa mã phụ trợ không dành riêng cho ứng dụng được sử dụng trong dự án.
Chúng tôi sẽ sử dụng nó để chứa mã có khả năng tái sử dụng như trình trợ giúp xác thực và mô hình cơ sở dữ liệu SQL
cho dự án.